# Environment variables for Portainer stack deployment
# When deploying via Repository in Portainer this file must be present in the repository
# Portainer will load these values into the stack environment automatically.

# When using Cloudflare Tunnel we do not want Caddy to perform ACME validation.
# Cloudflare will terminate TLS for clients and forward traffic to the local service.
CADDY_AGREE=false

# Keep ACME_EMAIL present for future use; not used when CADDY_AGREE=false
ACME_EMAIL=pinseekr@proton.me

# You can add further variables here if you update the compose file to reference them
# e.g. COMPOSE_PROJECT_NAME=pinseekr
